mode: live
dev_sleep_hours: 4
exchange: bitvavo
api_credentials: bitvavo_api_key.json
log_path: ../data/aurono_log.txt
db_path: ../data/trades.db
dashboard_host: 0.0.0.0
dashboard_port: 8000
live_trading: true

email:
  enabled: false           # controlled from Settings UI
  username: ""             # user email
  password: ""             # app password
  to: ""                   # normally same as username
  smtp_host: "smtp.gmail.com"  # static, no need to expose
  smtp_port: 587               # static, no need to expose
  from: "Aurono"               # static sender name

exchanges:
  bitvavo:
+    fee_rate: 0.0025   # 0.25% default estimate
  kraken:
+    fee_rate: 0.0025   # 0.25% default estimate




